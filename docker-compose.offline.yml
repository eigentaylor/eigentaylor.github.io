version: '3.8'

# Offline compose file: uses a locally-built/loaded image and does NOT pull from Docker Hub.
services:
  jekyll:
    image: local/al-folio:offline
    ports:
      - 8080:8080
      - 35729:35729
    volumes:
      - .:/srv/jekyll
    environment:
      - JEKYLL_ENV=development
